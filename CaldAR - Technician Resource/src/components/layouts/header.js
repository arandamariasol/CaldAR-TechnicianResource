import React from 'react';

function Header () {
    return (
        <header style={headerStyle}>
            <h1>Technicians
                
            </h1>
        </header>
    );
};

const headerStyle = {
    background: '#fc6225',
    color: '#fff',
    textAlign: 'center'
};

export default Header;
